using Obvious.Soap;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Europa
{
    //[CreateAssetMenu(fileName = "scriptable_list_" + nameof(FOESaveItem_Hybrid), menuName = "Soap/ScriptableLists/" + nameof(FOESaveItem_Hybrid))]
    public class ScriptableListFOEItem_Charm : ScriptableList<FOESaveItem_Hybrid>
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
